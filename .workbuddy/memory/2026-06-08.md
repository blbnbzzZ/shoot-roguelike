## 2026-06-08 开发日志

### 散弹枪增益系统实现
- 创建 `TripleShotPickup.gd` + `TripleShotPickup.tscn`：奖励房散弹枪3D模型拾取物
- 修改 `Player.gd`：添加 `triple_shot_enabled` 变量 + `triple_shot_activated` 信号，`_fire()` 支持三发扇形射击
- 修改 `Room.gd`：奖励房生成散弹枪拾取物
- 创建 `BuffUI.gd`：屏幕下方增益图标显示，鼠标悬停显示提示
- 修改 `Main.tscn` + `Main.gd`：添加 BuffUI 节点，连接信号

### 报错修复（信号/变量命名冲突）
- `Player.gd`：`signal triple_shot_enabled()` 与 `var triple_shot_enabled: bool` 同名导致 GDScript 解析失败
- 修复：信号重命名为 `triple_shot_activated()`
- `Main.gd`：同步更新信号连接和回调函数名
- `BuffUI.gd`：简化 `_ready()`，移除重复创建节点代码

### Boss房/奖励房连接逻辑修复
- `_find_boss_room()`：新增 `reward_idx` 参数，排除与奖励房直接相连的死路房间
- `_find_reward_room()`：排除与 Boss 房直接相连的房间
- 后处理：类型赋值前二次校验，若 Boss 房与奖励房仍相连则重新选房
- 效果：Boss 房和奖励房不再直接相邻，两者都只连接普通怪物房
- 提交：`bedb5a4`（逻辑修复）+ `79b0d04`（版本记录）

### Git 提交汇总（2026-06-08）
- `0e0c6d1`：实现散弹枪增益系统（拾取物+BuffUI+三发子弹）
- `880a1d3`：更新版本记录 0e0c6d1
- `bedb5a4`：修复 Boss 房与奖励房连接逻辑
- `a5dd0ab`：修复 BuffUI tooltip 错位：改为紧贴图标右侧垂直居中显示（使用 `top_level=true` + `global_position` 绝对定位）

### 房间大改（正方形化）
- 用户要求：所有房间改为正方形，小房间 640x640，大房间 1280x640（两个小房间合并），超大房间 1280x1280（四个小房间 2x2 拼成）
- `DungeonGenerator.gd`：`room_size` 改为 `Vector3(640, 0, 640)`
- `RoomTemplate.tscn`：小房间 640x640 正方形，16 处修改（地板、墙体、门、PlayerDetector）
- `RoomTemplateLarge.tscn`：大房间 1280x640，更新垂直墙体网格 200→280，门 Z 坐标 240→320，PlayerDetector Z 320，修复 LefT 重复 transform
- `RoomTemplateHuge.tscn`：超大房间从 2560x960 改为 1280x1280，更新所有墙体/门位置和尺寸
- `Room.gd`：更新 `large_door_positions`、`huge_door_positions`、敌人生成边界 `max_x/max_z`、地洞位置、奖励物品位置、Boss 出生位置
- 提交：`9881f43` 房间大改：所有房间模板改为正方形
